/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['miro.medium.com', 'www.freecodecamp.org'],
    // formats: ['image/avif', 'image/webp'],
  },
}

module.exports = nextConfig
